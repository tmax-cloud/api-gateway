http:
  # Add the router 
  routers: 
   my-router:
    entryPoints:
    - http 
    service: service-whoami
    rule: Path(`/whoami`)
  # Add the service 

  services: 
    service-whoami:
      loadBalancer:
        servers: 
        - url: http://192.168.9.105:5000/
          # -url: http://naver.com/
        passHostHeader: false

  